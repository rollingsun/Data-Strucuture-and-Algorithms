#include <iostream>
#include <stdlib.h>

void merge(int a[], m, l, h){
	if(l)
}

void mergeSort(int arr[],first, last){
		if(first<last){
		int mid=(start+last)/2;
		mergeSort(arr, first, mid)
		mergeSort(arr, mid+1, last)
		merge(arr,mid ,first, last)
		}
	}


using namespace std;